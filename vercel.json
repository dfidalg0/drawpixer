{
    "routes": [
        { "src": "/api/(.*)", "dest": "/api" },
        {
            "src": "/",
            "headers": {
                "X-Frame-Options": "DENY",
                "X-Content-Type-Options": "nosniff",
                "Referrer-Policy": "same-origin"
            }
        }
    ],
    "regions": ["gru1"]
}
